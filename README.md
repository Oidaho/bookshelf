# 📖 Bookshelf

![image](https://github.com/user-attachments/assets/9a382384-4062-48fc-877b-8d158d2cff28)

![GitHub Release](https://img.shields.io/github/v/release/Oidaho/bookshelf)

Bookshelf - REST API Сервис библиотеки, выполненный в рамках тестового задания от "DNS Teхнологии".

## ❗ Оглавление

- [🚨 Bookshelf](#-bookshelf)
  - [❗ Оглавление](#-оглавление)
  - [💡 Основные возможности](#-основные-возможности)
  - [📥 Установка](#-установка)
  - [📕 Документация](#-документация)

## 💡 Основные возможности

🔧 **CRUD-операции для ключевых сущностей**  
Полная поддержка операций создания, чтения, обновления и удаления для:

- **Авторов**.
- **Издательств**.
- **Книг**.
- **Читателей**.
- **Выдач книг**.

🔍 **Гибкий поиск и фильтрация**  
Мощные query-параметры позволяют настраивать листинги объектов с учетом:

- **Поиска** (фильтрации) по полям.
- **Сортировки**.
- **Пагинации**.

📊 **Автоматизированные отчеты**  

- **Просроченные выдачи**: Сервис формирует списки читателей, не вернувших книги в установленный срок.
- **Детализация по задолженностям**: дата выдачи, контакты читателя, информация о книге.

## 📥 Установка

### Системные зависимости

Перед тем, как коснуться темы развертывания, убедитесь, что у вас установлены необходимые системные зависимости:

- **Установить [Docker](https://docs.docker.com/get-started/get-docker/)**.
- **Установить [Docker-compose](https://docs.docker.com/compose/install/)**.
- **Установить [Python 3.13+](https://www.python.org/downloads/release/python-3130/)**

### Подготовка

Для развертывания проекта в режиме разработки или производства (production) необходимо выполнить следующие шаги.  
Первым делом установим Poetry:

```bash
pip install poetry
```

Склонируем репозиторий и перейдем в него:

```bash
git clone https://github.com/Oidaho/bookshelf
cd ./bookshelf
```

Как только мы оказались в директории проекта, следует установить все необходимые зависимости.  
Если вы желате продолжить разработку проекта, то вам необходимо установить все группы зависимостей:

```bash
poetry install --no-root
```

Если вы желаете настроить проект для производства (production) - пропустите этот шаг.

### Настройка окружения

Для того, чтобы проект успешно развернулся, необходимо указать значения переменных среды, а также сконфигуровать скрипт инициализации PGSQL.  
Создайте два файла в директории проекта:

```bash
touch .env
touch init.sql
```

В каждый из этих файлов нужно что-то поместить, начем с `init.sql`. Здесь будет содержаться скрипт создания пользователя, из-под которого `bookshelf` будет работать с СУБД.  
Вставьте в `init.sql` следующий скрипт:

```sql
CREATE USER bookshelf WITH PASSWORD 'password123';
CREATE DATABASE book_fund;
ALTER DATABASE book_fund OWNER TO bookshelf;
```

Вместо `password123` вставьте сгенерированный hex-пароль (можно воспользоваться [этим](https://www.browserling.com/tools/random-hex) сервисом), если вы собираетесь развертывать проект для производства (production).

Теперь откроем `.env` и вставим в него следующие строки:

```env
# Настройки проект
BOOKSHELF_DEBUG_MODE=True

# Настройка подключения к БД
BOOKSHELF_DB_POSTGRES_PASSWORD=password123
BOOKSHELF_DB_POSTGRES_HOST=localhost

# Настройка PGSQL
POSTGRES_PASSWORD=password123
```

Здесь все также: ничего не меняйте, если хотите использовать проект для локальной разработки.  
В случае, если вы хотите развернуть проект для производства (production), воспользуйтесь следующей таблицей переменных окружения:

| Переменная                     | Описание                                                                                                                                                                                                        | Стандартное значение | Статус       |
|--------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------|--------------|
| BOOKSHELF_DEBUG_MODE           | Переключатель дебаг-режима проекта. При развертывании в продакшене обязательно выставить False.                                                                                                                 |      BOOL: True      | Обязательная |
| BOOKSHELF_DB_POSTGRES_PASSWORD | Пароль пользователя bookshelf в СУБД PGSQL. При развертывании в продакшене обязательно смените на сгенерированный hex-пароль.                                                                                   |  STRING: password123 | Обязательная |
| BOOKSHELF_DB_POSTGRES_HOST     | Имя хоста, на котором развернут PGSQL. Если контейнер PGSQL находится на локальной машине и вы разработчик - оставьте localhost. В противном случае - смените на внутренний адрес контейнера из docker-compose. |   STRING: localhost  | Обязательная |
| POSTGRES_PASSWORD              | Пароль от админ-юзера PostgreSQL. Рекомендуется сгенерировать hex-пароль.                                                                                                                                       |  STRING: password123 | Обязательная |
| BOOKSHELF_DB_POSTGRES_USER     | Имя пользователя в СУБД PGSQL. Не рекомендуется менять, если вы не знаете, что делаете.                                                                                                                         |   STRING: bookshelf  | Опциональная |
| BOOKSHELF_DB_POSTGRES_PORT     | Порт, на котором работает контейнер PGSQL. Не рекомендуется менять, если вы не знаете, что делаете.                                                                                                             |     INTEGER: 5432    | Опциональная |
| BOOKSHELF_DB_POSTGRES_NAME     | Имя базы данных PGSQL, где будет работать bookshelf. Не рекомендуется менять, если вы не знаете, что делаете.                                                                                                   |   STRING: book_fund  | Опциональная |

### Настройка VSCode и разработка

Если вы разворачиваете проект для разработки - перезапустите VSCode, чтобы применить `.env`, если ранее вы проделывали все действия выше через VSCode.  
Также, возможно вы заметили, проект предлагает **_рекомендованные_**  расширения.
На данном этапе все готово к продолжению разработки.

Запустите контейнер с PGSQL на вашей локальной машине, используя команду:

```bash
docker-compose up -d --build postgres
```

После примените миграции `alembic`:

```bash
alembic upgrade head
```

Сгенерируйте фикстуры для базы данных (Этот шаг можно пропустить):

```bash
python .\scripts\create_db_fixtures.py

# Для очистки фикстур используйте
python .\scripts\clear_db.py
```

Запустите `FastAPI` приложение в отдельном инстансе терминала:

```bash
python .\app\start.py
```

Все готово!

### Развертывание в производство (production)

В предыдущем шаге была представлена конфигурация VSCode для разработки. В случае с продакшеном - этого делать не нужно.
У вас уже должны быть готовы файлы `init.sql` и `.env`. Если вы все еще этого не сделали... То как вы тут вообще оказались?  
Если все впорядке, то достаточно написать:

```bash
docker-compoes up -d
```

И сервис будет готов принимать запросы!  
Однако, перед тем как это делать, стоит изучить пару нюансов:

- **Миграции применяются на уровне контейнера**.
   Если вы не хотите применять миграции по каким-либо причинам, то переопределите команду
   запуска контейнера `bookshelf` при помощи `command` в `docker-compose`, указав запуск скрипта `start.py`
   без применения использования `alembic`.
- **Торчащий наружу порт**.
   Поумолчанию, в `docker-compoes` для контейнера СУБД указан порт `5432:5432`. Если вы не хотите, чтобы пор торчал на ружу,
   замените `5432:5432` на `5432`.

## 📕 Документация

Если вы хотите просмотреть документацию без развертывания проекта, перейдите в директорию:

```bash
cd ./bookshelf/docs
```

И запустите:

```bash
python -m http.server
```

Теперь по адресу `http://localhost:8000/` вам будет доступен SwaggerUI.

